# Reminders

Before acting, open the matching file in `~/.config/agentic/instructions/` or `~/.config/agentic/tools/` and follow it (routing table in `~/.config/agentic/AGENTS.md`).
Do not work from memory when a file exists for the topic.

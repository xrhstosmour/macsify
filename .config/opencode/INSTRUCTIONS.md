# Instructions

## Startup

1. Read ALL files in `~/.config/opencode/context/` folder, as your base knowledge for all interactions.
2. If project has AI config files (CLAUDE.md, AGENTS.md, .github/copilot-instructions.md, etc.), read those too.

## Workflow

/plan → /code → /test → /review
